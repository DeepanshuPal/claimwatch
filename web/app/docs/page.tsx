import type{Metadata}from"next";export const metadata:Metadata={title:"Docs"};export default function Page(){return <main className="doc"><div className="pagehead"><div className="eyebrow">Self-hosted first</div><h1>Run Watch My Handle anywhere.</h1><p>The Python CLI stores state in one JSON file and sends webhook or SMTP alerts. No Watch My Handle server is required.</p></div><h2>Install</h2><p><code>pip install -e .</code></p><h2>Use honest signals</h2><p>The claimwatch CLI reports <code>available</code>, <code>taken</code>, <code>unknown</code>, or <code>error</code>. Platforms that challenge requests or do not expose dependable public APIs stay unknown.</p><h2>Nearly $0</h2><p>Run it from cron or a scheduled GitHub Action. Persist <code>.claimwatch/state.json</code> between jobs.</p><p><a className="button" href="https://github.com/DeepanshuPal/claimwatch">Read the full README ↗</a></p></main>}
