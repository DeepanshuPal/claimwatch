import type{Metadata}from"next";import Checker from"../../components/Checker";export const metadata:Metadata={title:"Free name checker",description:"Check one name across social platforms, package registries, and common domains."};export default function Page(){return <main><div className="pagehead"><div className="eyebrow">Free instant checker</div><h1>Is your name still yours?</h1><p>Run live public checks across platforms and common TLDs. Results arrive as each source answers. Gray means the source could not prove either state.</p></div><Checker/></main>}
